# BetterThrowingSystem - Bug修复方案

## 问题1：按G导致角色死亡

### 问题分析
- 当前直接调用 `ChangeHoldItem(Item)` 方法
- 传入的物品可能是场景中的Clone，状态不正确
- 或者在特定状态下（如正在做动作）调用导致游戏逻辑错误

### 解决方案
**方案A：通过槽位切换（推荐）**
1. 找到投掷物所在的槽位编号（3-9号槽位）
2. 调用切换槽位的方法（模拟按数字键），而不是直接传入Item
3. 这样更符合游戏的原生逻辑，不会触发死亡

**方案B：状态检查**
1. 在调用 `ChangeHoldItem` 前检查玩家状态
2. 确保玩家处于可以装备物品的状态（不在战斗、不在动画中等）
3. 但这需要知道如何检查这些状态，可能比较复杂

## 问题2：复制投掷物

### 问题分析
- `FindObjectsOfType<Item>()` 会找到场景中所有Item，包括Clone
- `IsInPlayerCharacter` 对Clone也返回true
- 传入Clone给 `ChangeHoldItem` 会创建新实例

### 解决方案
**方案A：通过Inventory槽位获取物品（推荐）**
1. 不扫描场景中的Item，而是直接通过Inventory组件遍历槽位（3-9号）
2. 从槽位中直接获取Item引用
3. 只记录槽位编号，不存储Item引用

**方案B：过滤Clone**
1. 检查Item的name是否包含"(Clone)"
2. 或者检查Item的某些属性来区分原始物品和Clone
3. 但这可能不够可靠

## 推荐实现方案

### 步骤1：修改扫描逻辑
```csharp
// 不扫描场景Item，而是：
// 1. 获取玩家的Inventory组件
// 2. 遍历槽位3-9（投掷物通常在这些槽位）
// 3. 检查每个槽位中的物品是否为投掷物
// 4. 记录槽位编号，而不是Item对象
```

### 步骤2：修改装备逻辑
```csharp
// 不调用 ChangeHoldItem(Item)，而是：
// 1. 找到目标投掷物的槽位编号（如槽位3）
// 2. 调用切换槽位的方法，例如：
//    - UseSlot(3)
//    - EquipSlot(3)
//    - SwitchToSlot(3)
//    - 或者模拟按数字键3
```

### 步骤3：安全检查
```csharp
// 添加安全检查：
// 1. 确保玩家存在且有效
// 2. 确保槽位中有物品
// 3. 可能的话，检查玩家状态是否允许切换
```

## 需要的信息

### 1. Inventory槽位API
需要知道如何：
- 获取Inventory组件
- 获取槽位中的物品（GetItem(int slotIndex)）
- 获取槽位总数
- 检查槽位是否有物品

### 2. 槽位切换方法
需要找到：
- 切换槽位的方法名（UseSlot, EquipSlot等）
- 方法签名（参数类型）
- 是否在CharacterMainControl上还是Inventory上

### 3. 槽位范围
- 投掷物在哪个槽位范围（3-9？）
- 如何区分槽位是空的还是有物品的

## 下一步行动

1. **先添加调试代码**：在F9扫描中，尝试：
   - 获取Inventory组件
   - 遍历槽位3-9，打印每个槽位的物品
   - 找到切换槽位的方法

2. **然后实现新逻辑**：
   - 扫描时记录槽位编号（而不是Item对象）
   - 按G时根据槽位编号调用切换方法
   - 移除对ChangeHoldItem的直接调用

## 风险提示

⚠️ **不要直接修改代码**，先：
1. 查看日志，确认Inventory的API
2. 确认槽位切换方法
3. 在测试环境中验证方案
4. 然后才实施修复

