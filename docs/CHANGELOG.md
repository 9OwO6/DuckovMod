# 更新日志 / Changelog

## 版本 2.3.1 (2025-11-07)

### ✨ 主要更新

- **自动识别投掷物**：通过反射扫描 `SkillContext`、字段、属性与组件，自动将新出现的投掷物加入识别列表并同步 ModSetting 存档。
- **温雷系统升级**：新增碰撞继电器、延迟归零队列与日志诊断，让闪光手雷、管状炸弹等投掷物落地即爆，同时避免尚在手中时误爆。
- **ModSetting 同步改进**：识别列表实时刷新，所有设置启动时立即加载，Warm Grenade 等开关能在界面与逻辑中保持一致。

### 🛠️ 其他改动

- 增强 `prepare_for_upload.bat`，自动复制构建 DLL / 预览图，并指向主目录下的 `UploadReady` 文件夹。
- `ModSettingAPI.AddKeybinding` 增加调试日志，便于排查热键注册问题。

## 版本 2.1.0 (2025-11-02)

### 🎉 新功能

#### 1. **长按G键选择模式** ⭐
- **功能说明**：长按G键（0.3秒）进入投掷物选择模式
- **操作方式**：
  - 长按G键进入选择模式
  - 鼠标滚轮向上：切换到上一个投掷物种类
  - 鼠标滚轮向下：切换到下一个投掷物种类
  - 松开G键：装备当前选中的投掷物
- **智能切换**：按投掷物种类切换（而不是按背包槽位）
  - 例如：背包有 `雷、雷、火、火、火、闪`
  - 切换顺序：`雷 → 火 → 闪`（跳过重复种类）
- **视觉反馈**：
  - 选择模式中显示气泡提示：`"投掷物选择中，💣 [投掷物名称]"`
  - 选择完成时显示确认气泡：`"✓ 已选择：💣 [投掷物名称]"`
  - 支持图标指示器（如果有图标显示🎯，无图标显示💣）

#### 2. **智能记忆系统优化**
- **连续G键检测**：区分连续按G和中间穿插其他操作
- **投掷完成判断**：
  - 完成投掷后，连续按G会切换到下一个种类
  - 未完成投掷（取消/切换武器），再次按G会恢复上次选择
- **示例场景**：
  - `G（拿出雷）→ 右键取消 → 1（切武器）→ G（还是雷）` ✅
  - `G（拿出雷）→ 投掷 → G（拿出火）` ✅
  - `G（拿出雷）→ G（拿出火）→ G（拿出闪）` ✅

### 🐛 Bug修复

#### 1. **修复自动切回武器功能** 🔧
- **问题**：投掷完成后无法正确切回装备栏位的武器
- **原因**：之前错误地使用了背包槽位（Inventory），而不是装备槽位（Equipment Slots）
- **解决方案**：
  - 使用 `CharacterMainControl.SwitchHoldAgentInSlot(slotHash)` 切换装备槽位
  - 通过 `CharacterItem.Slots` 正确识别和保存装备槽位
  - 保存装备槽位的 `slotHash` 和 `Key`（"1", "2", "V"等）
- **技术改进**：
  - 使用 duckovAPI 推荐的 `CurrentHoldItemAgent.Item` 获取当前物品
  - 通过 `CharacterItem.Slots` 查找物品所在的装备槽位
  - 保存槽位的哈希值用于后续切换

#### 2. **修复投掷后自动切换投掷物的问题**
- **问题**：完成投掷后系统会自动切换到下一个投掷物，而不是等待用户按G
- **解决方案**：
  - 投掷完成后设置 `hasCompletedThrow = true`
  - 只有用户再次按G时才会切换到下一个种类
  - 如果用户在投掷后切换了武器，下次按G会恢复上次选择的投掷物

#### 3. **修复手动切换武器的误判**
- **问题**：自动切回武器时被误判为用户手动切换，导致记忆被清除
- **解决方案**：添加 `isThrowingInProgress` 和 `hasCompletedThrow` 标志，防止误判

### ⚙️ 技术改进

#### 1. **基于 duckovAPI 文档重构**
- 参考了完整的 duckovAPI 文档（来自 GitHub）
- 使用推荐的API方法：
  - `CurrentHoldItemAgent.Item` - 获取当前手持物品
  - `CharacterItem.Slots` - 访问装备槽位系统
  - `SwitchHoldAgentInSlot(slotHash)` - 切换装备槽位

#### 2. **代码结构优化**
- 分离了装备槽位和背包槽位的处理逻辑
- 添加了详细的状态追踪变量
- 改进了错误处理和日志记录

#### 3. **性能优化**
- 优化了投掷物扫描逻辑
- 改进了气泡显示机制（避免重复创建）

---

## 版本 2.0.0 (之前版本)

### 基础功能
- ✅ 按G键快速切换投掷物
- ✅ 按TypeID分类切换投掷物
- ✅ 投掷完成后自动切回武器
- ✅ 智能记忆系统（记住上次选择的投掷物）
- ✅ 投掷检测系统（通过物品数量变化）

---

## 开发计划 / Development Roadmap

### 🔜 即将推出的功能

#### 1. **UI管理界面** (功能 #1)
- [ ] 创建Mod设置UI界面
- [ ] 开关选项：
  - 启用/禁用自动切回武器
  - 启用/禁用长按G选择模式
  - 调整长按时间阈值
  - 启用/禁用气泡提示
- [ ] 快捷键自定义
- [ ] 投掷物黑名单/白名单管理

#### 2. **直接投掷模式** (功能 #2)
- [ ] 按G直接投掷模式
- [ ] 投掷到鼠标悬停位置
- [ ] 快速投掷（无需瞄准）

#### 3. **音效系统** (功能 #3)
- [ ] 投掷音效（默认：鸭子叫声 🦆）
- [ ] 自定义音效支持
- [ ] 不同投掷物不同音效

### 🐛 待修复问题

#### 1. **堆叠Mod兼容性** (Bug #3)
- [ ] 修复堆叠物品的装备问题
- [ ] 处理堆叠物品的投掷计数

#### 2. **优化切换顺序** (Bug #5)
- [ ] 优化按类别切换的逻辑
- [ ] 支持用户自定义切换顺序

### 💡 未来可能的功能

- [ ] 投掷物快捷键绑定（1-9键快速选择）
- [ ] 投掷物使用统计
- [ ] 自动补充投掷物（从仓库）
- [ ] 投掷物预设配置
- [ ] 多人游戏支持优化

---

## 技术细节

### API依赖
- **duckovAPI**: 使用了 CharacterMainControl、Item、Inventory、Slot 等核心API
- **ItemStatsSystem**: 用于物品识别和属性访问
- **Unity Input System**: 用于输入检测

### 兼容性
- ✅ 兼容游戏最新版本
- ✅ 兼容其他Mod（不修改游戏核心文件）
- ⚠️ 堆叠Mod需要额外测试

### 已知问题
- 某些特殊投掷物可能无法被正确识别（已添加TypeID黑名单机制）
- 在极高延迟情况下，投掷检测可能略有延迟

---

## 贡献者

- **开发者**: Leo
- **AI助手**: Auto (Cursor AI)

---

## 反馈与支持

如果遇到问题或有功能建议，请在Steam Workshop页面留言或提交Issue。

感谢使用 BetterThrowingSystem！🎮


